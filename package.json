{
  "name": "datasecuretools-proje",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "generate-types": "wrangler types",
    "deploy": "npm run build && npx wrangler pages deploy dist --project-name=datasecuretools-proje"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.1.0",
    "@astrojs/sitemap": "^3.7.1",
    "astro": "^6.0.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260310.1",
    "miniflare": "^4.20260310.0",
    "wrangler": "^4.72.0"
  }
}
